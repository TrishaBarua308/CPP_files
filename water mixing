#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t; cin>>t;
	while(t--)
	{
	    int a,b,x,y;   cin>>a>>b>>x>>y;
	    int d = abs(a-b);
	    
	    if(a==b) cout<<"YES\n";
	    else if(a<b && d<=x)   cout<<"YES\n";
	    else if(a>b && d<=y )  cout<<"Yes\n";
	    else cout<<"NO\n";
	}

}
