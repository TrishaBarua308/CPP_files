#include <bits/stdc++.h>
using namespace std;

int main() 
{
	// your code goes here
	int t;  cin>>t;
	while(t--)
	{
	    int x,y,a,b;  cin>>x>>y>>a>>b;
	    int f = max(x,y);
	    int t = min(x,y);
	    int ff = max(a,b);
	    int tt = min(a,b);
	    
	    if(f==ff && t == tt) cout<<0<<endl;
	    else if(f==ff || f==tt || t==tt || t==ff) cout<<1<<endl;
	    else cout<<2<<endl;
	}

}
