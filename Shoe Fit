#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t; cin>>t;
	while(t--)
	{
	    int a,b,c;  cin>>a>>b>>c;
	    int mn = min(a, min(b,c));
	    int mx = max(a, max(b,c));
	    if(mn==0 && mx==1) cout<<1<<endl;
	    else cout<<0<<endl;
	}

}
