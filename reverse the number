#include <bits/stdc++.h>
using namespace std;
     
int main() {
    /**
    * Write your C++ code from here
    */

    int n,sum=0;  cin>>n;

    while(n!=0)
    {
        int rem = n%10; //4
        sum = sum*10 + rem;
        n=n/10;
    }

    cout<<sum<<endl;

    return 0;
}
