#include <bits/stdc++.h>

using namespace std;

int main() 
{
    // your code goes here
    int t;  cin>>t;
    while(t--)
    {
        int l,r;  cin>>l>>r;
        int ans=0;
        for(int i=l; i<=r; i++)
        {
            if(i%10==2) ans++;
            if(i%10==3) ans++;
            if(i%10==9) ans++;
        }
        cout<<ans<<endl;
    }
}
