#include <bits/stdc++.h>
#define ll long long
using namespace std;
     
int main() {
    /**
    * Write your C++ code from here
    */
    ll n;  cin>>n;

    if(n%2==0) cout<<n<<" is an even number "<<endl;
    else       cout<<n<<" is an odd number."<<endl;

    return 0;
}
