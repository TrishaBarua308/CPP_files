#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;   cin>>t;
	while(t--)
	{
	    int n,x,p;  cin>>n>>x>>p;
	    int fail = n-x; 
	    int total = (x*3) - fail;
	    if(total >=p) cout<<"PASS\n";
	    else cout<<"FAIL\n";
	}
}
