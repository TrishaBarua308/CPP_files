// https://atcoder.jp/contests/abc296/tasks/abc296_a
#include <bits/stdc++.h>
#define optimize()                \
    ios_base::sync_with_stdio(0); \
    cin.tie(0);                   \
    cout.tie(0);
using namespace std;
int main()
{
    optimize();
    int n;
    cin >> n;

    string s;
    cin >> s;
    bool ans = true;

    for (int i = 0; i < n - 1; i++)
    {
        if (s[i] == s[i + 1])
        {
            cout << "No\n";
            return 0;
        }
    }

    cout << "Yes\n";
}
