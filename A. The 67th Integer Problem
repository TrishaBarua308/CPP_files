#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;   cin>>t;
    
    while(t--)
    {
        int x;   cin>>x;
        if(x>0 && x<67) cout<<x+1<<"\n";
        else    cout<<x<<"\n";
    }
    
    return 0;
}
