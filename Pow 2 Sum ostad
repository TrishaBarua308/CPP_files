#include <bits/stdc++.h>
#define ll long long
using namespace std;
     
int main() 
{

    ll n;  cin>>n;
    int count=0;

    while(n>0)
    {
        if(n%2==1)
        {
            count++;
        }

        n/=2;
    }

    cout<<count<<endl;

    return 0;
}
