#include <bits/stdc++.h>
using namespace std;

int main() 
{
	// your code goes here
	int t;  cin>>t;
	while(t--)
	{
	    int x,y;   cin>>x>>y;
	    int d = x+y;
	    int f = ( 500 - (x*2) ) + ( 1000 - (d*4) );
	    int t =  ( 1000 - (y*4) ) + ( 500 - (d*2) );
	    
	    if(f <= t) cout<<t<<endl;
	    else cout<<f<<endl;
	    
	}

}
