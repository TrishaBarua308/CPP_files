#include <bits/stdc++.h>
using namespace std;
     
int main() {
    /**
    * Write your C++ code from here
    */
    string s; cin>>s;
    char temp = s[0];

    for(int i=0; i<4; i++)
    {
        
        s[i] = s[i+1];
    }
    s[5-1] = temp;

    cout<<s<<endl;

    return 0;
}
