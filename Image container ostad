#include <bits/stdc++.h>
using namespace std;
     
int main() {
    /**
    * Write your C++ code from here
    */
    
    int n;  cin>>n;
    if(n<1000 && n%10==0) cout<<(1000-n)/2<<endl;
    else cout<<0<<endl;

    return 0;
}
