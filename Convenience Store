#include <bits/stdc++.h>
using namespace std;
     
int main() 
{
    double q,d,n,p, total;      
    cin>>q>>d>>n>>p>>total;

    double ans = (q*0.25)+(d*0.10)+(n*0.05)+(p*0.01);

    if(total <= ans)  cout<<"true"<<endl;
    else cout<<"false"<<endl;
    

    return 0;
}
